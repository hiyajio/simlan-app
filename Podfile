# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Simlan' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for Simlan
  pod 'Firebase'
  pod 'Firebase/Auth'
  pod 'Alamofire'
  
  target 'SimlanTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'SimlanUITests' do
    # Pods for testing
  end
  
end
